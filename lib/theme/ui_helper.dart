import 'package:flutter/material.dart';

class UIHelper {
  static const crossFadeDuration = Duration(milliseconds: 300);

  static const floatingLocationRegisterAlert =
      FloatingActionButtonLocation.centerTop;
}
